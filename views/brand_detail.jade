extends layout

block content
  h1= title
  h3 Brand: #{brand.name}
  hr
  h4 Items
  dl
      each item in brand_items
        dt
          a(href=item.url) #{item.name}
        dd #{item.description}

      else
        p This brand has no items
